me_pattern_for = {
	icon = 8
	pattern_type = for
	
	default = {
		character = FROM # Actor
	}
	slavery_2 = {
		OR = {
			trait = slave_owner
			trait = slave_holder
		}
	}
	slavery_3 = {
		OR = {
			trait = slave_owner
			trait = slave_holder
		}
	}
}

me_pattern_against = {
	icon = 9
	pattern_type = against
	slavery_0 = {
		OR = {
			trait = slave_owner
			trait = slave_holder
		}
	}
}