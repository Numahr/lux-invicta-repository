namespace = fafscript

# Elect trait check
character_event = {
	id = fafscript.0
	desc = fafscript.0.desc	
	picture = GFX_evt_emissary
	
	 hide_window = yes
	
	trigger = {
	    age = 16
		OR = {
			religion = mani
			religion = mandaean
			religion = mandaean_reformed		}
	    is_theocracy = yes
	    NOT = {trait = manichean_elect}
	}
	
	
	mean_time_to_happen = {
		days = 1
    }
	
	immediate = {
		add_trait = manichean_elect
	}
	
	option = {
		name = OK
	}
}

